<%= form_for(@to_done) do |f| %>
  <% if @to_done.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@to_done.errors.count, "error") %> prohibited this to_done from being saved:</h2>

      <ul>
      <% @to_done.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
